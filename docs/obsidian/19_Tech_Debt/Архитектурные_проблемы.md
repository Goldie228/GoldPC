# 🏗️ Архитектурные проблемы (остаток)

> **Раздел**: 19_Tech_Debt | **Версия**: 3.0
> **Статус**: 11 из 13 исправлены

---

### 🟡 1. ReadOnlyCatalogDbContext — наследование от write-контекста

**Файлы**: `src/CatalogService/Data/ReadOnlyCatalogDbContext.cs`

Наследует `CatalogDbContext` через `CreateBaseOptions` workaround. Для курсового проекта — приемлемо.

**Оценка**: ~12 часов. Выделить общий базовый класс с конфигурациями сущностей.

---

### 🟢 2. User.Roles — Ignored в EF Core (не баг)

**Файлы**: `src/AuthService/Data/AuthDbContext.cs`

```csharp
entity.Ignore(e => e.Roles); // UserRole — enum, не entity
```

`UserRole` — enum, а не entity. Текущий подход (computed `Role` property) корректен. Добавление many-to-many потребует превращения `UserRole` в entity.

**Статус**: Не баг, feature-request для production.

---

## 🔗 Связанные страницы

- [[19_Tech_Debt/Проблемы_масштабирования]] — риски масштабирования
- [[19_Tech_Debt/Обзор_техдолга]] — общая сводка
