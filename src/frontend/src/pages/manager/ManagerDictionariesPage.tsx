/**
 * Страница справочников для менеджера
 * Переиспользует компонент из админки (DRY)
 */
import { DictionariesPage } from '@/pages/admin/dictionaries-page/DictionariesPage';

export function ManagerDictionariesPage() {
  return <DictionariesPage />;
}
