apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: goldpc
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
</task_progress>
</write_to_file>