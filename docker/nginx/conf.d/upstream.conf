# =============================================================================
# Upstream Configuration for Blue-Green Deployment
# =============================================================================
# This file is AUTO-GENERATED by the Blue-Green deployment workflow.
# Do NOT edit manually - changes will be overwritten during deployment.
#
# Current Active Slot: blue
# Last Updated: Initial configuration
# =============================================================================

# Catalog Service - Blue Slot (Primary)
upstream backend_catalog {
    server catalog-blue:5001;
    keepalive 32;
    
    # Health check parameters (for NGINX Plus)
    # health_check interval=30s fails=3 passes=2;
}

# PC Builder Service - Blue Slot (Primary)
upstream backend_pcbuilder {
    server pcbuilder-blue:5002;
    keepalive 32;
}

# Auth Service - Blue Slot (Primary)
upstream backend_auth {
    server auth-blue:5003;
    keepalive 32;
}

# Frontend - Blue Slot (Primary)
upstream frontend_upstream {
    server frontend-blue:8080;
    keepalive 32;
}

# =============================================================================
# Green Slot Configuration (Inactive - ready for deployment)
# =============================================================================
# Uncomment to switch to Green slot manually (not recommended)
# 
# upstream backend_catalog {
#     server catalog-green:5011;
#     keepalive 32;
# }
# 
# upstream backend_pcbuilder {
#     server pcbuilder-green:5012;
#     keepalive 32;
# }
# 
# upstream backend_auth {
#     server auth-green:5013;
#     keepalive 32;
# }
# 
# upstream frontend_upstream {
#     server frontend-green:8080;
#     keepalive 32;
# }